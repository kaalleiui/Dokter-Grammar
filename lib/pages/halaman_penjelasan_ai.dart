import 'package:flutter/material.dart';

class HalamanPenjelasanAi extends StatelessWidget {
  const HalamanPenjelasanAi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Penjelasan AI')),
      body: const Center(child: Text('Ini adalah halaman Halaman Penjelasan AI')),
    );
  }
}
