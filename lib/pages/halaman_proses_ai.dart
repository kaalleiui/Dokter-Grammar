import 'package:flutter/material.dart';

class HalamanProsesAi extends StatelessWidget {
  const HalamanProsesAi({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Proses AI')),
      body: const Center(child: Text('Ini adalah halaman Halaman Proses AI')),
    );
  }
}
