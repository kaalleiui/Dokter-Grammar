import 'package:flutter/material.dart';

class HalamanLatihanHarian extends StatelessWidget {
  const HalamanLatihanHarian({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Latihan Harian')),
      body: const Center(child: Text('Ini adalah halaman Halaman Latihan Harian')),
    );
  }
}
