import 'package:flutter/material.dart';

class HalamanLatihanCustom extends StatelessWidget {
  const HalamanLatihanCustom({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Latihan Custom')),
      body: const Center(child: Text('Ini adalah halaman Halaman Latihan Custom')),
    );
  }
}
