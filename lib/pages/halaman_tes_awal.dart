import 'package:flutter/material.dart';

class HalamanTesAwal extends StatelessWidget {
  const HalamanTesAwal({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Halaman Tes Awal')),
      body: const Center(child: Text('Ini adalah halaman Halaman Tes Awal')),
    );
  }
}
